answer(b, ok).
why(
  answer(b, ok),
  proof(
    goal(answer(b, ok)),
    by(rule("027_materialize_excludes_source_fact.pl", clause(5))),
    uses([
      proof(
        goal(seed(a)),
        by(fact("027_materialize_excludes_source_fact.pl", clause(2)))
      )
    ])
  )
).

