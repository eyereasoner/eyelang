# Eyelog 0.7.0

Since 0.6.20, Eyelog adds supported explanation output via `--explain` for materialized `triple/3` results and `--query` answers, showing original rules, substitutions, and proof structure while keeping normal output unchanged by default.
